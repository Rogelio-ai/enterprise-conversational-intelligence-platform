route:
  receiver: operator-pending
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: operator-pending
    # OPERATOR INPUT REQUIRED: configure a protected webhook/email receiver before pilot GO.
